server:
  port: 0

spring:
  application:
    name: member
  datasource:
    url: jdbc:mysql://k8e103.p.ssafy.io:3306/member?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
    username: paymong103
    password: paymong103.!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        use_sql_comments: true

eureka:
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}}
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka

header:
  member: "MemberId"
  mong: "MongId"

logging:
  level:
    org:
      hibernate: info
